# LinuxHUD 架构概览

## 一句话定位
**绕过整个图形栈，通过 DRM/KMS 直接在显卡硬件叠加平面上渲染信息，实现绝对优先级的平视显示器。**

## 核心架构图

```
┌─────────────────────────────────────────────────────────────┐
│                      用户空间 (User Space)                    │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │   AI/监控    │  │   配置管理   │  │   日志系统   │       │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘       │
│         │                 │                 │               │
│         ▼                 ▼                 ▼               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              LinuxHUD 守护进程 (linuxhud)            │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │   │
│  │  │ 渲染引擎    │  │ 内存管理    │  │ 资源管理    │ │   │
│  │  │ (Cairo)     │  │ (Buffer)    │  │ (DRM)       │ │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘ │   │
│  └─────────────────────────────────────────────────────┘   │
│                           │                                 │
└───────────────────────────┼─────────────────────────────────┘
                            │ 系统调用 (ioctl)
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                      内核空间 (Kernel Space)                  │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐   │
│  │              DRM/KMS 子系统                          │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │   │
│  │  │   Plane     │  │   CRTC      │  │ Connector   │ │   │
│  │  │   Manager   │  │   (显示控制) │  │ (物理接口)  │ │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘ │   │
│  └─────────────────────────────────────────────────────┘   │
│                           │                                 │
└───────────────────────────┼─────────────────────────────────┘
                            │ 驱动程序
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                      硬件层 (Hardware)                        │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐   │
│  │                  GPU 硬件                             │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │   │
│  │  │ Overlay     │  │ Primary     │  │ Cursor      │ │   │
│  │  │ Plane       │  │ Plane       │  │ Plane       │ │   │
│  │  │ (HUD显示)   │  │ (桌面显示)  │  │ (鼠标显示)  │ │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘ │   │
│  └─────────────────────────────────────────────────────┘   │
│                           │                                 │
└───────────────────────────┼─────────────────────────────────┘
                            │ 信号输出
                            ▼
                      ┌───────────┐
                      │   显示器   │
                      └───────────┘
```

## 数据流

```
┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐
│ AI/监控 │───▶│ 文本生成 │───▶│ Cairo   │───▶│ Dumb    │───▶│ DRM     │
│ 数据    │    │         │    │ 渲染    │    │ Buffer  │    │ Commit  │
└─────────┘    └─────────┘    └─────────┘    └─────────┘    └─────────┘
                                                                  │
                                                                  ▼
                                                            ┌─────────┐
                                                            │ 硬件显示 │
                                                            └─────────┘
```

## 核心代码模块

```
linuxhud/
├── src/
│   ├── main.c              # 主入口、守护进程逻辑
│   ├── drm_device.c        # DRM 设备操作
│   │   ├── open_device()   # 打开 /dev/dri/card0
│   │   ├── find_plane()    # 发现 Overlay Plane
│   │   └── get_resources() # 获取显示资源
│   ├── buffer.c            # 显存管理
│   │   ├── create_dumb()   # 创建 Dumb Buffer
│   │   ├── mmap_buffer()   # 映射到用户空间
│   │   └── destroy_buffer()# 释放缓冲区
│   └── renderer.c          # 渲染引擎
│       ├── init_cairo()    # 初始化 Cairo
│       ├── draw_text()     # 渲染文本
│       └── commit_frame()  # 提交帧
├── include/
│   └── linuxhud.h          # 公共头文件
└── tests/
    └── test_overlay.c      # 测试程序
```

## 关键技术点

### 1. Overlay Plane 优先级
```
硬件叠加平面 (Hardware Overlay)
    ↓ 优先级最高
主平面 (Primary Plane)
    ↓ 正常桌面
光标平面 (Cursor Plane)
    ↓ 鼠标指针
```

### 2. 内存映射
```c
// 用户态直接写入显存
void *mapped = mmap(NULL, size, PROT_READ | PROT_WRITE, 
                    MAP_SHARED, fd, offset);
// 直接写入像素
memset(mapped, 0xFF, size);  // 白色填充
```

### 3. 双缓冲
```
Buffer A ← 当前显示
Buffer B ← 下一帧渲染

// 渲染完成后交换
drmModeSetPlane(fd, plane_id, crtc_id, 
                buffer_b_fb_id, ...);
```

## 环境要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Linux (内核 4.8+) |
| 权限 | video 组或 root |
| 依赖库 | libdrm, cairo, pango, glib |
| 显卡驱动 | 支持 DRM/KMS 的开源驱动 |

## 快速验证

```bash
# 检查 DRM 设备
ls -la /dev/dri/

# 查看显卡信息
sudo drm_info

# 检查 Overlay Plane
sudo cat /sys/class/drm/card1/device/planes
```

## 与网络数据面的类比

| 网络概念 | 图形概念 | 说明 |
|----------|----------|------|
| XDP/eBPF | DRM/KMS | 绕过协议栈，直接操作硬件 |
| 网卡队列 | CRTC | 硬件控制器 |
| 数据包 | 像素数据 | 最小处理单元 |
| DMA 缓冲区 | Dumb Buffer | 直接内存访问 |
| 零拷贝 | 内存映射 | 避免数据复制 |
